{
  "name": "salonrink-sync-bot",
  "version": "1.0.0",
  "description": "SALON BOARD reservation sync bot with Railway cron",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "test:connectivity": "ts-node src/test-connectivity.ts",
    "test:curl": "ts-node src/test-curl.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "playwright": "^1.59.1",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "ts-node": "^10",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
